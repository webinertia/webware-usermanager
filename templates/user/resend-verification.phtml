<?php

declare(strict_types=1);

$this->headTitle('Resend Verification Email');
?>
<div class="d-flex align-items-center justify-content-center min-vh-100 bg-body-secondary p-3">
    <div class="ims-login-wrap">
        <div class="card shadow-lg w-100">
            <div class="row g-0">

                <!-- Brand panel (desktop left column) -->
                <div class="col-lg-5 ims-login-brand-panel d-none d-lg-flex flex-column align-items-center justify-content-center p-5 text-center">
                    <i class="bi bi-boxes mb-3 ims-login-brand-icon"></i>
                    <div class="fw-bold fs-3 lh-sm mb-2">Farmers IMS</div>
                    <div class="mb-4 ims-login-brand-tagline">Inventory Management System</div>
                    <ul class="list-unstyled text-start ims-login-brand-features">
                        <li class="mb-2"><i class="bi bi-check2-circle me-2"></i>Scan &amp; track every incoming manifest</li>
                        <li class="mb-2"><i class="bi bi-check2-circle me-2"></i>Damage reports with photo evidence</li>
                        <li class="mb-2"><i class="bi bi-check2-circle me-2"></i>Ticket &amp; transfer workflows</li>
                        <li class="mb-0"><i class="bi bi-check2-circle me-2"></i>Cross-store inventory visibility</li>
                    </ul>
                </div>

                <!-- Content panel -->
                <div class="col-12 col-lg-7">
                    <div class="p-4 p-lg-5">

                        <!-- Mobile brand (hidden on desktop) -->
                        <div class="d-flex align-items-center justify-content-center gap-2 mb-4 d-lg-none">
                            <i class="bi bi-boxes fs-2 text-primary"></i>
                            <span class="fs-4 fw-bold">Farmers IMS</span>
                        </div>

                        <?php if (!empty($this->sent)): ?>

                            <div class="d-flex align-items-center gap-3 mb-4">
                                <i class="bi bi-envelope-check fs-1 text-success"></i>
                                <div>
                                    <h1 class="h4 fw-bold mb-1">Check Your Inbox</h1>
                                    <p class="text-secondary small mb-0">A new verification link has been sent.</p>
                                </div>
                            </div>

                            <div class="alert alert-success" role="alert">
                                If that email address is registered and awaiting verification, you will receive a new link shortly. The link expires in 24&nbsp;hours.
                            </div>

                        <?php elseif (!empty($this->error)): ?>

                            <div class="d-flex align-items-center gap-3 mb-4">
                                <i class="bi bi-envelope-x fs-1 text-danger"></i>
                                <div>
                                    <h1 class="h4 fw-bold mb-1">Resend Failed</h1>
                                    <p class="text-secondary small mb-0">We could not send a new verification email.</p>
                                </div>
                            </div>

                            <div class="alert alert-danger" role="alert">
                                <?= $this->escapeHtml($this->error) ?>
                            </div>

                            <form action="<?= $this->userUrl('resend.verification.create') ?>" method="post" novalidate>
                                <div class="mb-3">
                                    <label class="form-label form-label-sm fw-semibold" for="resend-email">Email</label>
                                    <input
                                        type="email"
                                        class="form-control"
                                        id="resend-email"
                                        name="email"
                                        value="<?= $this->escapeHtml($this->email ?? '') ?>"
                                        placeholder="you@farmers-store.com"
                                        autocomplete="email"
                                        required
                                    />
                                </div>
                                <button type="submit" class="btn btn-primary w-100 mb-3">Resend Verification Email</button>
                            </form>

                        <?php else: ?>

                            <div class="d-flex align-items-center gap-3 mb-4">
                                <i class="bi bi-envelope-arrow-up fs-1 text-primary"></i>
                                <div>
                                    <h1 class="h4 fw-bold mb-1">Resend Verification Email</h1>
                                    <p class="text-secondary small mb-0">Enter your email to receive a new link.</p>
                                </div>
                            </div>

                            <form action="<?= $this->userUrl('resend.verification.create') ?>" method="post" novalidate>
                                <div class="mb-3">
                                    <label class="form-label form-label-sm fw-semibold" for="resend-email">Email</label>
                                    <input
                                        type="email"
                                        class="form-control"
                                        id="resend-email"
                                        name="email"
                                        placeholder="you@farmers-store.com"
                                        autocomplete="email"
                                        required
                                    />
                                </div>
                                <button type="submit" class="btn btn-primary w-100 mb-3">Resend Verification Email</button>
                            </form>

                        <?php endif; ?>

                        <div class="text-center">
                            <a href="<?= $this->userUrl('session.read') ?>" class="small text-secondary">Already verified? Sign in</a>
                        </div>

                    </div>
                </div>

            </div>
        </div>
    </div>
</div>
